A really basic docker-compose based stack

# Usage
The started stack expose Grafana UI to localhost:3000

* init user: `admin`
* init password: `admin`


```bash
# starting the stack
./bin/docker-compose.sh start

```